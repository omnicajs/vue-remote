---
title: Worker Kanban Sandbox
description: A live sandbox that runs a sortable kanban board in a dedicated Web Worker while the docs page hosts the real DOM tree.
template: doc
---

import WorkerKanbanSandbox from '../../components/WorkerKanbanSandbox.astro'

## How this example is built

This page shows a full worker-based setup without mocks. The docs page owns the host DOM and pointer hit-testing, while the remote Vue app and kanban board state run inside a dedicated `Web Worker`.

The example has three parts:

- a host runtime that mounts `HostedTree` into the preview area;
- a remote worker app that renders lanes, cards, and handles `RemoteSortableEvent`;
- a live snapshot below the board that shows the current remote state after each drop.

## How to use it

- Grab cards only by the `Drag` handle.
- Move between cards in a lane to reorder, or into another lane to transfer a card.
- Use `Reset board` to return the demo to its initial state.

<WorkerKanbanSandbox
  codeCssLabel="Sandbox CSS"
  codeHostLabel="Host runtime code"
  codeWorkerLabel="Worker remote code"
  draggingLabel="Dragging"
  instructions="Drag cards by their handle, reorder them inside a lane, move them across lanes, and reset the example whenever you want to start over."
  previewTitle="Sortable kanban board running in a dedicated Worker"
  resetLabel="Reset board"
  runtimeNote="This board is wired through the same host/worker transport model used by the library's public runtime APIs."
  snapshotLabel="Live remote snapshot"
  status={{
    booting: 'Booting dedicated Worker runtime...',
    failed: 'Worker sandbox failed',
    ready: 'Running in a dedicated Worker runtime',
    snapshotUnavailable: 'Remote snapshot is unavailable.',
    unsupported: 'This browser does not support module workers.',
  }}
/>

## Related pages

- [Sortable Drag and Drop](/sortable-dnd/)
- [Web Worker Runtime](/web-workers/)
- [Remote Components](/remote-components/)
